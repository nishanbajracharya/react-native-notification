import {StyleSheet} from 'react-native';

import color from './constants/colors';

const style = StyleSheet.create({
  view: {
    backgroundColor: color.APP_BG_COLOR
  },
  text: {
    color: color.APP_TEXT_COLOR
  },
  textInput: {
    margin: 10
  }
});

export default style;
